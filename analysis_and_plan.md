# 📊 การวิเคราะห์ข้อมูลและแผนงานฐานข้อมูลหลัก (Master Database Plan)

## 1. บทสรุปผู้บริหาร

โฟลเดอร์ `drive_assets` มีไฟล์ทั้งหมด **299 ไฟล์** ซึ่งผสมผสานไปด้วยไฟล์จากระบบจัดการโรงแรม, การเงินส่วนบุคคล, การตั้งค่าระบบ และไฟล์เบ็ดเตล็ดอื่นๆ ข้อมูลในปัจจุบันมีความกระจัดกระจายและมีหลายเวอร์ชัน (เช่น `(1)`, `(2)`, `สำเนาของ...`) รวมถึงมีการตั้งชื่อไฟล์ที่หลากหลาย (เช่น `DB-Hotel...`, `VIPAT...`, `ระบบจัดการโรงแรม...`)

**เป้าหมาย:** รวบรวมข้อมูลที่กระจัดกระจายเหล่านี้ให้เป็น **ฐานข้อมูลหลักใหม่ (New Master Database)** เพียงแหล่งเดียว เพื่อความถูกต้องของข้อมูล ลดความซ้ำซ้อน และง่ายต่อการจัดการในอนาคต

---

## 2. การจัดหมวดหมู่ไฟล์ (File Categorization)

เราได้วิเคราะห์และจัดกลุ่มไฟล์ออกเป็นหมวดหมู่ดังนี้:

### 🏨 กลุ่ม A: ระบบจัดการโรงแรม (ข้อมูลหลัก/Core Data)

*สำคัญที่สุดสำหรับฐานข้อมูลหลัก*

* **ข้อมูลลูกค้า/ผู้เข้าพัก:** `DB-โรงแรม - 2-1 ลูกค้า.csv`, `DB-Hotel-FullSystem - 2-1 ลูกค้า.csv`, `รายชื่อผู้ตรวจสอบ.csv`
* **การจอง & การเข้าพัก:** `DB-โรงแรม - 2-2 จองห้อง.csv`, `Booking_Report...`, `Check-in Data.xlsx`, `ทะเบียนผู้เข้าพัก...`
* **ห้องพัก & คลังห้อง:** `DB-โรงแรม - 1-2 ห้อง.xlsx`, `Data_Room.xlsx`, `Master Data System.xlsx`
* **การตั้งค่า:** `1-1 ตั้งค่า.xlsx`, `ระบบจัดการโรงแรม VIPAT - ⚙️ Config.csv`

### 💰 กลุ่ม B: การเงินและบัญชี (ธุรกิจ)

* **รายรับ:** `DB-โรงแรม - 2-3 รายได้อื่นๆ.csv`, `ใบแจ้งค่าเช่าห้อง...`, `ใบเสร็จรับเงิน...`
* **รายจ่าย & ต้นทุน:** `DB-โรงแรม - 2-4 รายจ่าย.csv`, `Budget Planner...`, `ระบบสร้างใบเสร็จรับเงินอัตโนมัติ.xlsx`, `Invoice Run...`
* **เงินเดือน & บุคคล:** `Employee Details.xlsx`, `งานโรงแรม_ค่าเช่า_เพย์โรล.xlsx`, `สลิปเงินเดือน...`, `ส่งประกันสังคม...`
* **รายงาน:** `รายงานขาย AllCafe...`, `งบประมาณธุรกิจประจำปี...`

### 👤 กลุ่ม C: ส่วนตัวและประสิทธิภาพการทำงาน

* **การเงินส่วนบุคคล:** `ai_studio_code.csv`, `Template Simple Expense Tracker...`, `เครื่องมือติดตามการลงทุน...`
* **การจัดการงาน:** `7 Days Task Tracking...`, `Project Management.xlsx`, `Monthly Task and Mission Tracker..`
* **อื่นๆ:** `Books.xlsx`, `Visitors.xlsx`

### ⚙️ กลุ่ม D: ระบบและไฟล์ชั่วคราว

* **ไม่มีชื่อ/ชั่วคราว:** `สเปรดชีตไม่มีชื่อ...` (หลายไฟล์), `ทำไงต่อ.xlsx`, `เอาให้ Ai เลยนะ.xlsx`
* **สำรองข้อมูล/ซ้ำ:** ไฟล์ที่ลงท้ายด้วย `(1)`, `(2)`, `สำเนาของ...`

---

## 3. สถาปัตยกรรมฐานข้อมูลหลักใหม่ที่นำเสนอ

เราขอเสนอให้สร้างโครงสร้างโฟลเดอร์ใหม่เพื่อเก็บข้อมูลที่รวบรวมแล้ว เพื่อให้เป็น "Single Source of Truth" (แหล่งข้อมูลความจริงเพียงแหล่งเดียว)

### 📂 โครงสร้างโฟลเดอร์ที่แนะนำ

เพื่อความเป็นสากลและรองรับระบบโปรแกรมในอนาคต ขออนุญาตใช้ชื่อโฟลเดอร์เป็นภาษาอังกฤษ แต่คำอธิบายจะเป็นภาษาไทยครับ

```text
/Master_Database_New
├── 📂 01_Core_Data          (ข้อมูลหัวใจหลักของระบบ)
│   ├── Master_Guests.csv    (รายชื่อลูกค้าทั้งหมด - รวมจากทุกไฟล์)
│   ├── Master_Rooms.csv     (ข้อมูลห้องพักและสถานะ)
│   ├── Master_Users.csv     (รายชื่อพนักงาน/ผู้ใช้งานระบบ)
│   └── Master_Config.csv    (การตั้งค่าต่างๆ, ตัวเลือก Dropdown)
│
├── 📂 02_Operations         (การดำเนินงานรายวัน)
│   ├── Master_Bookings.csv  (ข้อมูลการจองทั้งหมด)
│   ├── Master_CheckIns.csv  (ข้อมูลการเข้าพักปัจจุบัน - Active)
│   └── Housekeeping_Log.csv (ประวัติแม่บ้าน/ทำความสะอาด)
│
├── 📂 03_Finance            (ข้อมูลการเงิน)
│   ├── Master_Transactions.csv (รวมรายรับ/รายจ่าย ทั้งหมดในที่เดียว)
│   ├── Payroll_Data.csv     (ข้อมูลเงินเดือน)
│   └── Invoices_Archive/    (ที่เก็บไฟล์ใบแจ้งหนี้เก่า)
│
└── 📂 04_Archive_Old        (ย้ายไฟล์เก่าทั้งหมดมาเก็บที่นี่)
    ├── /Legacy_VIPAT        (ไฟล์จากระบบ VIPAT เดิม)
    ├── /Legacy_DB_Hotel     (ไฟล์จาก DB-Hotel เดิม)
    └── /Misc_Backups        (ไฟล์สำรองอื่นๆ)
```

### 🗄️ รูปแบบข้อมูลที่เสนอ (Schema ของไฟล์หลัก)

#### 1. `Master_Guests.csv` (รวมข้อมูลจาก `DB-ลูกค้า`)

| ชื่อคอลัมน์ (En) | ประเภท | คำอธิบาย (Th) |
| :--- | :--- | :--- |
| `guest_id` | PK | รหัสลูกค้า (Unique ID) เช่น G-0001 |
| `full_name` | Text | ชื่อ-นามสกุล |
| `phone` | Text | เบอร์โทรศัพท์ |
| `national_id` | Text | เลขบัตรประชาชน / Passport |
| `address` | Text | ที่อยู่ |
| `country` | Text | สัญชาติ |
| `created_at` | Date | วันที่สร้างข้อมูล |

#### 2. `Master_Bookings.csv` (รวมข้อมูลจาก `DB-จองห้อง`)

| ชื่อคอลัมน์ (En) | ประเภท | คำอธิบาย (Th) |
| :--- | :--- | :--- |
| `booking_id` | PK | รหัสการจอง เช่น BK-202511-001 |
| `guest_id` | FK | ลิงก์ไปยังข้อมูลลูกค้า (Master_Guests) |
| `room_id` | FK | ลิงก์ไปยังข้อมูลห้อง (Master_Rooms) |
| `check_in_date` | Date | วันที่เช็คอิน |
| `check_out_date` | Date | วันที่เช็คเอาท์ |
| `status` | Enum | สถานะ (Confirmed, Checked-in, Checked-out, Cancelled) |
| `channel` | Text | ช่องทางจอง (Agoda, Walk-in, Line, ฯลฯ) |
| `total_amount` | Money | ยอดเงินรวม |

#### 3. `Master_Transactions.csv` (รวม `รายจ่าย`, `รายรับ`)

| ชื่อคอลัมน์ (En) | ประเภท | คำอธิบาย (Th) |
| :--- | :--- | :--- |
| `trans_id` | PK | รหัสธุรกรรม เช่น TR-001 |
| `date` | Date | วันที่ |
| `type` | Enum | ประเภท (Income-รายรับ / Expense-รายจ่าย) |
| `category` | Text | หมวดหมู่ (เช่น ค่าเช่าห้อง, ค่าไฟ, ค่าซ่อมแซม) |
| `amount` | Money | จำนวนเงิน |
| `related_booking_id`| FK | (เผื่อเลือก) ลิงก์กับ Booking |
| `description` | Text | รายละเอียดเพิ่มเติม |

---

## 4. ขั้นตอนถัดไป (Action Plan)

1. **ยืนยันแผนงาน:** ท่านเห็นชอบกับโครงสร้างนี้หรือไม่?
2. **สร้างโครงสร้าง:** ผมจะสร้างโฟลเดอร์ `/Master_Database_New` และสร้างไฟล์ CSV เปล่าพร้อมหัวตาราง (Header)
3. **ย้ายและรวมข้อมูล (Migration):**
    * เขียนโปรแกรมอ่านไฟล์ `DB-ลูกค้า` (คัดกรองชื่อซ้ำ) และนำไปใส่ `Master_Guests.csv`
    * เขียนโปรแกรมอ่าน `DB-จองห้อง` และนำไปใส่ `Master_Bookings.csv` (แปลงรูปแบบวันที่ให้เป็นมาตรฐานเดียวกัน)
    * ตั้งค่าข้อมูลห้องพักใน `Master_Rooms.csv`
4. **ทำความสะอาด (Cleanup):** ย้ายไฟล์เก่า/ไฟล์ซ้ำ ไปเก็บใน `/Master_Database_New/04_Archive_Old` เพื่อให้หน้างานสะอาดและเป็นระเบียบ

**ท่านต้องการให้เริ่มดำเนินการ "สร้างโครงสร้างและรวมข้อมูล" ตามนี้เลยหรือไม่ครับ?**
